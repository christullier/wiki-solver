include some context for each article
* add a new context variable to article class?

Add link checking on input
* that way it won't return a weird json error for not recognizing the link

Make it check for less than 60 days

Use the # of backlinks to a page as the value instead of views

use pickling to save the paths already used
